# PocketBase Configuration
# Copy this file to .env and update with your values

# Port for PocketBase server
PORT=8090

# PocketBase version (check latest at: https://github.com/pocketbase/pocketbase/releases)
PB_VERSION=0.23.4

# Encryption key for PocketBase (generate a random string)
# Generate with: openssl rand -hex 32
# IMPORTANT: Keep this secret and never commit it to git
PB_ENCRYPTION_KEY=your-secret-encryption-key-here-generate-with-openssl-rand-hex-32

# Admin UI URL (local)
# ADMIN_UI_URL=http://localhost:8090/_

# API Base URL (local)
# API_BASE_URL=http://localhost:8090

# Production URLs (uncomment and update for production)
# ADMIN_UI_URL=https://projects.volleypass.com/_
# API_BASE_URL=https://projects.volleypass.com
